class example_fibo {
    def main() {
        for (i=0;i<5;i++)
            print(i);
    }
               
    def fib(x) {        
        if (x == 0) return 0;
        if (x == 1) return 1;
        
        return fib(x-1) + fib(x-2);
    }
}
 