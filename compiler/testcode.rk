include "a.rky";

class foo {
    def _ctor() {
        a = string.new("ASDF");
        b = string.new("WORLD");

        a.append(b);
        print(a[1]);
        print(a.to_string());
    }
}

class bar {
    def hello() {
        print("hello");
    }
} 

