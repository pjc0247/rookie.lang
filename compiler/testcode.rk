class op_priority_test {
    @main
    static def main() {
        a = idx.new();

        print(b);
    }
}

class idx {
    def __getitem__ (x) { 
        return x;
    }
    def __setitem__ (x, y){ 
        print(x)
        print(y)
    }
}