class example_object {
    @main
    static def main() {
        f = foo.new();
        f.v = 1234;
        f.v = null;
    }
}

class foo {
    def say_hello() {
        hello = "hello";

        for (i=0;i<hello.length();i++)
            print(hello[i]);

        print(this.v);
    }
}
