class example_fibo {
    @main
    def main2() {
        a = "21234";
        for (i=0;i<a.length();i++)
            print(a[i]);
    }
               
    def fib(x) {        
        if (x == 0) return 0;
        if (x == 1) return 1;
        
        return fib(x-1) + fib(x-2);
    }
}
 