// MUST NOT execute a body
// if condition is false initially
class for_test {
	@main
    static def main() {
        a = [1,2,3];

        for (c in a)
            puts (c);
        
        puts ("PASS");
    }
}