#pragma once

#include "value_object.h"

inline value operator+(const value &a, const value &b);
inline value operator-(const value &a, const value &b);
inline value operator*(const value &a, const value &b);
inline value operator/(const value &a, const value &b);

