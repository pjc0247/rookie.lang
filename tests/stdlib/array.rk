class ary_test {
	@main
    static def main() {
        a = [1,2,3,4];
        puts(a.length());

        a.clear();
        puts(a.length());

        a = [1,2,3];
        puts(a[0]);
        puts(a[1]);
        puts(a[2]);

        puts(a.at(0));
        puts(a.at(1));
        puts(a.at(2));
    }
}