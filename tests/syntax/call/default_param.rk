class default_param_test {
    @main
    static def main () {
        foo();
        foo(10);
        foo(10, 20);
    }

    static def foo (a = 1, b = 2) {
        puts (a);
        puts (b);
    }
}