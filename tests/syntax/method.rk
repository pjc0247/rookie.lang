class dog {
    def kind() {
        puts("animal");
    }
}
class anchovy {
    def kind() {
        puts("fish");
    }
}

class callstatic_test {
    @main
    static def main() {
        // animal
        dog.new().kind();

        // fish
        anchovy.new().kind();
    }
}
