class dog {
    static def kind() {
        puts("animal");
    }
}
class anchovy {
    static def kind() {
        puts("fish");
    }
}

class callstatic_test {
    @main
    static def main() {
        // animal
        dog.kind();

        // fish
        anchovy.kind();
    }
}
